spring:
  application:
    name: account-service
  cloud:
    config:
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
  main:
    allow-bean-definition-overriding: true
